const axios = require('axios');

async function test() {
  try {
    // Attempt to login as staff. We need credentials. Let's try default user/pass.
    // If we don't know, we can check DB.
  } catch (err) {
    console.error(err);
  }
}
test();
